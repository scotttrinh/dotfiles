# OMP Home Module v17.0.0 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade the `omp` Nix module to support the `oh-my-pi` v17.0.0 settings schema changes and clean up obsolete options.

**Architecture:** Modify `modules/home/omp/default.nix` to remove obsolete options (`discoveryMode` and `essentialOverride`), add new options (`xdevEnabled` and `enforceSeenLines`), map them to the proper generated configuration fields, and update the memory backend enum.

**Tech Stack:** Nix, Home Manager

## Global Constraints
- Target file: `modules/home/omp/default.nix`
- Obsolete options `discoveryMode` and `essentialOverride` must be completely removed.
- `memory.backend` enum must contain exactly `["off" "local" "hindsight" "mnemopi"]`.

---

### Task 1: Update OMP Module Settings & Mappings

**Files:**
- Modify: `modules/home/omp/default.nix`

**Interfaces:**
- Consumes: None
- Produces: Updated Home Manager options and config generation for `omp` v17.0.0

- [ ] **Step 1: Apply surgical edits to modules/home/omp/default.nix**

Apply the following modifications to `modules/home/omp/default.nix`:
1. In `typedSettings.edit` mapping (around line 441), add:
   ```nix
      enforceSeenLines = cfg.files.enforceSeenLines;
   ```
2. In `typedSettings.tools` mapping (around line 500), remove `discoveryMode = cfg.tools.discoveryMode;` and `essentialOverride = cfg.tools.essentialOverride;` and add `xdev = cfg.tools.xdevEnabled;`.
3. In `options.omp.memory.backend` (around line 910), change:
   ```nix
        backend = enumOption [
          "off"
          "local"
          "hindsight"
          "mnemopi"
        ] "Memory backend.";
   ```
4. In `options.omp.files` (around line 925), add `enforceSeenLines = nullableOption types.bool "Gate the hashline seen-line guard.";` after `blockAutoGenerated`.
5. In `options.omp.tools` (around line 971), remove the `discoveryMode` and `essentialOverride` option definitions, and add `xdevEnabled = nullableOption types.bool "Mount rarely-used tools under xd:// device URLs.";` after `maxTimeout`.

- [ ] **Step 2: Run formatting tool**

Run `nix fmt` in the project root to format the changed nix files.

Expected: Files are formatted cleanly.

- [ ] **Step 3: Run Nix evaluation check**

Run: `nix flake check`
Expected: PASS with no syntax or evaluation errors.

- [ ] **Step 4: Commit the changes**

```bash
git add modules/home/omp/default.nix
git commit -m "feat(omp): upgrade home module options and mappings to v17.0.0"
```
